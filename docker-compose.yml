version: '2'

services:
    selhost:
        build:
            context: .
            dockerfile: DockerfileSel-Host
        ports:
            - "4444:4444"  
        tty: true
        volumes:
        - ./Shared:/tmp/Shared
    selnode1:
        build:
            context: .
            dockerfile: DockerfileSel-Node
        links:
        - selhost
        tty: true
    selnode2:
        build:
            context: .
            dockerfile: DockerfileSel-Node
        links:
        - selhost
        tty: true
    selnode3:
        build:
            context: .
            dockerfile: DockerfileSel-Node
        links:
        - selhost
        tty: true
    selnode4:
        build:
            context: .
            dockerfile: DockerfileSel-Node
        links:
        - selhost
        tty: true
